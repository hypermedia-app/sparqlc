{
  "name": "migrate",
  "private": "true",
  "scripts": {
    "get-raw": "dotenv -- r2rml4net -c \"Server=$SERVER;Database=$DATABASE;User Id=$USERNAME;Password=$PASSWORD\" -m mapping.ttl -o out/raw.nq"
  },
  "dependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
